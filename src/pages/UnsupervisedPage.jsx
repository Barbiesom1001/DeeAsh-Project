import React, { useState } from 'react';
import { motion, AnimatePresence } from 'framer-motion';
import { Users, PieChart as PieIcon, ActivitySquare, TrendingUp, AlertCircle, Leaf } from 'lucide-react';
// ... (ใส่ Logic และ UI ของหน้า Unsupervised ที่คุณมี)
const UnsupervisedPage = () => {
    return (
        <div className="space-y-6">
            <h2 className="text-2xl font-serif text-[#1A1A1A]">Customer Segmentation</h2>
            {/* ใส่ UI ส่วน Persona Cards และ PCA ตรงนี้ */}
        </div>
    );
};
export default UnsupervisedPage;